/** @type {import('next').NextConfig} */
const nextConfig = {
  reactStrictMode: true,
  images: {
    domains: ['rb.gy', 'upload.wikimedia.org', 'clipartmax.com'],
  },
}

module.exports = nextConfig
